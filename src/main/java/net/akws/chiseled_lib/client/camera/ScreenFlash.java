package net.akws.chiseled_lib.client.camera;

import net.minecraft.util.Identifier;

public class ScreenFlash {
/*
    public int fullDuration;
    public int duration;
    public int alpha;
    public Identifier texture;
    public boolean removed = false;

    public ScreenFlash(int fd, int d, int alpha, Identifier texture) {
        this.alpha = alpha;
        this.texture = texture;
        this.duration = d;
        this.fullDuration = fd;
    }

    public void tick() {
        if (this.duration <= 0 && !this.removed) {
            this.removed = true;
        } else {
            if (this.alpha >= 1) {
                this.alpha--;
            }
        }
    }*/

}
